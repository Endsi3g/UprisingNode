# Database (PostgreSQL - use Supabase, Railway, Neon, or AWS RDS)
DATABASE_URL="postgresql://user:password@host:5432/uprising_node?schema=public"

# JWT Configuration
JWT_SECRET="your-super-secret-jwt-key-change-in-production-minimum-32-chars"

# Server Configuration
PORT=3000
NODE_ENV=development

# CORS
CORS_ORIGIN=http://localhost:3001

# Optional: Email service for password reset (future)
# SMTP_HOST=smtp.example.com
# SMTP_PORT=587
# SMTP_USER=user@example.com
# SMTP_PASSWORD=your-smtp-password
# SMTP_FROM=noreply@uprisingnode.com
